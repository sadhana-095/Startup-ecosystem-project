import dynamic from 'next/dynamic'

// Dynamically import the HomeContent component with no SSR
// This prevents any client-side code from running during server render
const HomeContent = dynamic(() => import('./home-content'), { ssr: false })

export default function HomePage() {
  return <HomeContent />
}
